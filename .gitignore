# Keep .gitignore minimal per request to track all project files,
# including ssl/, cache/, and env files. Only ignore OS/editor artifacts.

# System files
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
